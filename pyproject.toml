[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "data_curation"
version = "0.1.0"
description = "A suite of tests for data curation"
requires-python = ">=3.9"
dependencies = [
    "pandas>=2.3.0",
]
